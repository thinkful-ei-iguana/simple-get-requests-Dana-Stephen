'use strict';

const numberOfPics = function () {
  $('user-input').on('click', event => {
    let num = $('input').val();
    return num;
  });
};

const getDoggos = function (num) {
  
  fetch(https://dog.ceo/api/breeds/image/random/45)
}